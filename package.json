{
  "name": "@kanselarij-vlaanderen/au-kaleidos-icons",
  "version": "3.0.1",
  "description": "Kaleidos icons as a separate package. This package builds an icon font",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "icon-font": "fantasticon",
    "prepare": "npm run build",
    "build": "npm run clean && rimraf iconfont && mkdirp iconfont && npm run icon-font"
  },
  "keywords": [
    "icons",
    "svg",
    "icon set"
  ],
  "author": "Mono",
  "license": "MIT",
  "devDependencies": {
    "fantasticon": "^1.2.3",
    "mkdirp": "^1.0.4",
    "rimraf": "^3.0.2"
  }
}
